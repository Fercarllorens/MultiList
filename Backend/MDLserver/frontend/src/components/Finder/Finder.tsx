import Search_bar from "../Search_bar/Search_bar"
import './Finder.css'

const Finder = () => {
    return (
        <div className="container">
            <Search_bar />
        </div>
    )
}

export default Finder
